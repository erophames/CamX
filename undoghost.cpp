/*

#include "undo.h"
#include "object_track.h"
#include "gui.h"

void Undo::AddUTrack(Seq_Track *track)
{
	if(track->olist!=&tracks)
		tracks.AddEndO(track);
}

void Undo::CutUTrack(Seq_Track *track)
{
	if(track->olist==&tracks)
		tracks.CutQObject(track);
}

*/
